import styled from "styled-components";

export const StyledButton = styled.button`
  width: 90%;
  background: var(--red);
  padding: 0.75rem;
  border-radius: 50px;
  border: none;
  font-size: 1.15rem;
  font-weight: 900;
  color: white;
`;
