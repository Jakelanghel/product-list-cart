import styled from "styled-components";

export const StyledButton = styled.button`
  width: 90%;
  background: var(--red);
  padding: 1rem;
  border-radius: 50px;
  border: none;
  font-size: 1rem;
  font-weight: 900;
  color: white;
`;
